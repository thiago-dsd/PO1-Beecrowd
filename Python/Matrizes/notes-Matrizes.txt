Formas de criara matriz
1 - criar uma única linha e cria a coluna após isso (sl3)
2 - criar uma matriz chamando as linhas e colunas. abrindo e fechando os colchetes.


primeiro defina o número de linhas e depois o numero de colunas, exempos sl4
~as colunas são criada no for, as linhas são determinadas no vetor inicial

~Python sempre imprime uma matriz como uma lista de vetores

LEN MAT - retorna a quantidade de listas dentro da lista principal

captando entrada de dados
1) captar o num de linahs e colunas
2) crie uma matriz com o num de linhas e colunas

